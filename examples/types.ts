import type { Operation, OpStep, VizEvent, StateSnapshot } from '@ltvis/shared';

// Re-export shared contracts so examples can reference a single source of truth.
export type { Operation, OpStep, VizEvent, StateSnapshot };
