{
  "name": "@ltvis/web",
  "version": "0.0.0",
  "private": true,
  "description": "LT-Vis web app (stub scaffold)",
  "scripts": {
    "dev": "echo \"stub: web dev\"",
    "build": "tsc -p ../tsconfig.json --noEmit || true",
    "typecheck": "tsc -p ../tsconfig.json --noEmit || true",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p ../tsconfig.json --noEmit || true"
  },
  "dependencies": {
    "@ltvis/shared": "workspace:*",
    "@ltvis/model-ts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
